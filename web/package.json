{
  "name": "meetsign-web",
  "private": true,
  "scripts": {
    "dev:admin": "pnpm --filter admin dev",
    "dev:checkin": "pnpm --filter checkin dev",
    "build": "pnpm -r build",
    "build:deploy": "pnpm --filter admin build && pnpm --filter checkin build"
  }
}
